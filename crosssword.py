def set_value(crossword, i, value):
    return NotImplemented


def dp(crossword):
    # find the line with least number of possible value
    # for optin in optins: dp(set_value(crossword, line, optin)
    return NotImplemented


def solve(crossword):
    # convert start info into table of possible values
    return dp(crossword)
